# SonarCloud Project Configuration
sonar.projectKey=anhkhoa289_vote-anti-trick
sonar.organization=anhkhoa289

# Project metadata
sonar.projectName=vote-anti-trick
sonar.projectVersion=0.1.0

# Source code settings
sonar.sources=app,components,lib,types

# Test settings (currently no tests in project)
# sonar.tests=tests

# Exclusions
sonar.exclusions=**/*.config.js,**/*.config.ts,node_modules/**,prisma/generated/**,.next/**,out/**,coverage/**,public/**

# Coverage settings (optional - requires test coverage report)
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
# sonar.typescript.lcov.reportPaths=coverage/lcov.info

# Language settings
sonar.language=ts
sonar.sourceEncoding=UTF-8

# Branch analysis settings
# Allow analysis of all branches (not just main)
sonar.branch.autoconfig.disabled=false

# Pull request analysis settings
sonar.pullrequest.github.repository=anhkhoa289/vote-anti-trick
